[pytest]
# 1. 自动模式：自动识别 async def 函数为异步测试
asyncio_mode = auto

# 2. 设置默认的 Event Loop 作用域
# 既然你的 client fixture 用了 module 作用域，这里建议设为 module 或 session
asyncio_default_fixture_loop_scope = module

# 3. 忽略一些无关紧要的警告（可选，但这能让输出清爽点）
filterwarnings =
    ignore::DeprecationWarning