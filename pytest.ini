[pytest]
# 1. 自动模式：自动识别 async def 函数为异步测试
asyncio_mode = auto

# 2. 设置默认的 Event Loop 作用域
# 🟢 [修复] 将其从 module 改为 session
# 解释: 因为我们在 conftest.py 中有一个 scope="session" 的 prepare_database fixture，
# Event Loop 的生命周期必须至少跟它一样长，否则会报 ScopeMismatch。
asyncio_default_fixture_loop_scope = session

# 3. 忽略一些无关紧要的警告（可选，但这能让输出清爽点）
filterwarnings =
    ignore::DeprecationWarning