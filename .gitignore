# =================================================================
# 1. 关键的秘密和私有数据 (MOST IMPORTANT!)
# =================================================================
# 绝对不要上传你的 API Keys 和环境配置！
# 你的 DeepSeek Key 就在这里面。
.env
.env.*
!.env.example


# =================================================================
# 2. 生成的数据库 (DATABASES)
# =================================================================
# 永远不要上传你的向量数据库，它们会变得非常大，而且是生成的。
# (根据你的项目截图)
chroma_langchain_db/
*.sqlite3


# =================================================================
# 3. Python 虚拟环境 (VIRTUAL ENVIRONMENTS)
# =================================================================
# 永远不要上传你的虚拟环境，它包含几百个包。
# 别人应该用 'requirements.txt' 来重建环境。
venv/
.venv/
llm_env/
env/
.env/


# =================================================================
# 4. Python 缓存和编译文件 (PYTHON CACHE)
# =================================================================
# 这些是 Python 运行时自动生成的字节码缓存
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.whl


# =================================================================
# 5. Jupyter Notebook 检查点 (JUPYTER)
# =================================================================
# 这是 Jupyter 的自动备份，不需要上传。
.ipynb_checkpoints/


# =================================================================
# 6. IDE / 编辑器配置 (EDITOR - VS Code)
# =================================================================
# 这是你本地 VS Code 的设置，团队其他人不需要。
# (注意：.vscode/settings.json 有时会共享，但全忽略最安全)
.vscode/


# =================================================================
# 7. 模型和数据缓存 (CACHE)
# =================================================================
# 比如你刚下载的 Qwen 模型就存在 .cache 里 (虽然在系统盘，但以防万一)
# 或者其他本地缓存
.cache/
hf_cache/
.huggingface/


# =================================================================
# 8. 操作系统生成的文件 (OS - Windows)
# =================================================================
# Windows 缩略图缓存和桌面配置
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# =================================================================
# 9. 
# =================================================================

chromadb/

*.csv

data/

embed_models/

logs/
*.log

debug_*